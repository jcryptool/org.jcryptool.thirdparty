#!/usr/bin/env bash

bindir="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")"
rootdir="$bindir/.."

"$bindir/publish-p2" "releng/org.jcryptool.thirdparty.m2bundle.p2/target/repository" "$rootdir/../org.jcryptool.thirdparty.p2/m2bundle" "$bindir/gitPush"


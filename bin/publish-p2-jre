#!/usr/bin/env bash

bindir="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")"
rootdir="$bindir/.."

"$bindir/publish-p2" "releng/org.jcryptool.thirdparty.jre.p2/target/repository" "$rootdir/site/jre" "$bindir/gitPush"


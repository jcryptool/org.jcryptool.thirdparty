#!/usr/bin/env bash

bindir="$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")"
rootdir="$bindir/.."

# "$bindir/publish-p2" "releng/org.jcryptool.thirdparty.m2bundle.p2/target/repository" "$rootdir/site/m2bundle" "$bindir/gitPush"
(cd releng/org.jcryptool.thirdparty.platform.p2; mvn -Dclean.skip=true jetty:run; )

